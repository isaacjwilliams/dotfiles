# Use the Secret Service (gnome-keyring) for saved passwords.
# Chrome does not auto-detect a keyring when XDG_CURRENT_DESKTOP=Hyprland,
# so it must be named explicitly or Chrome falls back to the plaintext
# "basic" store.
--password-store=gnome-libsecret
